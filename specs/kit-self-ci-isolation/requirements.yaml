# FEAT-002 — 要件の単一の正。実装・テスト・レビューはこれに従属する。
id: FEAT-002
title: "conformance-kit自身のCIをサンプル検査から分離する"
status: frozen # draft -> (spec-lint --gate freeze) -> frozen

intent: "conformance-kitのメンテナが、キット自身のリポジトリのCI(gateチェック)をbranch
  protectionのrequired statusにできるようにする。install.sh --with-samples
  で配布するために同梱している、意図的に違反を含むサンプル(specs/example-login/, src/auth.ts, src/rogue.ts,
  tests/auth.test.ts)がキット自身のconformance検査に混入して恒久的に失敗するのを止め、キット自身の実コンテンツにのみ基づいて意\
  味のあるpass/failを返すようにする。ただしサンプルの回帰検知(install.shが壊れていないか、検出ロジックが機能し続けているか)は非必須ジョ\
  ブとして維持する。"

out_of_scope:
  - "scripts/*.ts（trace-matrix.ts、spec-lint.tsを含むキット本体のロジック）をconformance.config\
    .jsonのsrcDirsに加えて本格的にドッグフーディング対象にする拡張（別機能として扱う）"
  - "install.shの引数体系・フラグ名（--with-samplesという名称自体を含む）の変更"
  - "サンプルが内包する意図的な違反シナリオの中身（どのACが何の理由で落ちるか）の変更"
  - "GitHub Actions以外のCIプロバイダへの対応"
  - "install.sh --with-samples を過去に実行済みの外部ターゲットリポジトリに対する移行ガイド・自動マイグレーションの提供"

assumptions:
  - id: AS-001
    statement: "サンプル一式の新しい格納場所は examples/with-samples/
      とし、元のディレクトリ構造（specs/example-login/, src/, tests/）をそのまま配下に再現する（例:
      examples/with-samples/specs/example-login/requirements.yaml）"
    status: confirmed
    owner: kohei-dev
    resolve_by: "2026-09-12"
  - id: AS-002
    statement: "conformance.config.json の specDir(=specs) / srcDirs(=src, tests)
      は変更しない。examples/ を新たに ignore
      に加える必要はない（specDir/srcDirsの外にあるため、既存の発見ロジック上そもそもスキャン対象に入らない）。\
      [追記 2026-09-24] srcDirsの不変はFEAT-008（specs/kit-self-dogfooding/）で上書きされ、scriptsとinstall.mjsが追加された。\
      examples/をスキャン対象外とする点は引き続き有効"
    status: confirmed
    owner: kohei-dev
    resolve_by: "2026-09-19"
  - id: AS-003
    statement: "サンプルの回帰検知は .github/workflows/conformance.yml 内に非必須ジョブ
      samples-selfcheck を追加して維持する。このジョブは continue-on-error: true とし、branch
      protection の required status checks には含めない"
    status: confirmed
    owner: kohei-dev
    resolve_by: "2026-09-12"
  - id: AS-004
    statement: "origin/main と origin/master
      のデフォルトブランチ不一致は、.github/workflows/conformance.yml のtrace-matrix呼び出し（push時の
      --base ${{ github.base_ref || 'main' }}
      フォールバック）だけでなく、conformance.config.json の baseRef(現在 origin/main) と
      scripts/trace-matrix.ts の DEFAULTS.baseRef(現在 origin/main)
      の計3箇所に存在する。この3箇所すべてを origin/master に修正する"
    status: confirmed
    owner: kohei-dev
    resolve_by: "2026-09-19"
  - id: AS-005
    statement: "samples-selfcheck ジョブが examples/with-samples/ を対象に trace-matrix.ts
      を実行できるよう、trace-matrix.ts に `--root <dir>` オプションを新設する。指定時は (1)
      conformance.config.json を `<dir>/conformance.config.json`
      から読み込み、specDir・srcDirsの探索基準を `<dir>` にする、(2) git diffは実行時のリポジトリルートから `git
      diff --name-only --relative=<dir> --diff-filter=d <baseRef>...HEAD`
      として取得し、`<dir>` 配下のファイルを `<dir>`
      相対パスで得ることで、ファイル探索側のパスと突合せの基準を一致させる。spec-lint.ts は既存の位置引数（例: `spec-lint.ts
      examples/with-samples/specs`）でそのまま対応可能なため変更しない"
    status: confirmed
    owner: kohei-dev
    resolve_by: "2026-09-19"

open_questions:
  - id: OQ-001
    from: AC-007
    question: "2回の修正で AC-007 が満たせない。受入基準自体が誤っている / 前提が欠けている可能性がある。最新の失敗:
      最新masterコミット(f5992f0, AS-002/004/005確定コミット)に対するGitHub
      Actions実行でgateジョブがfailureとして表示されている。traceabilityステップがAC-002/005/008のAC_FA\
      ILINGでexit1になるため。前回検証時と同根の失敗が継続中"
    raised: 2026-09-12
    status: resolved
  - id: OQ-002
    from: AC-008
    question: "2回の修正で AC-008 が満たせない。受入基準自体が誤っている / 前提が欠けている可能性がある。最新の失敗: required
      status checksがそもそも有効化されていない。thenが要求する『required status
      checksの一覧にgateを含む』を満たさない。またAC-007のgateがまだ連続成功していないため、givenの前提自体も未成立"
    raised: 2026-09-12
    status: resolved
  - id: OQ-003
    from: AC-005
    question: "2回の修正で AC-005 が満たせない。受入基準自体が誤っている / 前提が欠けている可能性がある。最新の失敗:
      AC-007/AC-008が2回目のfailでblockedへ降格しopen_questionsに2件起票されたため、frozenなFEAT-00\
      2にopen_questionsが残る状態になりspec-lint.tsのFROZEN_WITH_OPENでgate.shが最初の段階(1/4)で\
      停止する。すべてのコマンドがexit0にならず、thenを満たさない"
    raised: 2026-09-12
    status: resolved
  - id: OQ-004
    from: AC-002
    question: "2回の修正で AC-002 が満たせない。受入基準自体が誤っている / 前提が欠けている可能性がある。最新の失敗:
      rogue.ts・example-login由来のfinding、NO_BASE_REF警告は解消済みでAS-004/AC-006の修正効果は確認\
      できる。しかしthenは『exit
      codeが0になる』も要求しており、同一spec内のAC-005/007/008がblocked(2回目fail後の自動降格)であるため、それらの\
      AC_BLOCKED/FROZEN_WITH_OPENでexit1が継続。AC-005/007/008がすべてpassしopen_question\
      sが解消しない限り構造的にfailし続ける。これはtrace-matrix.ts自体の欠陥ではなく、AC-005/007/008という未解決の外部\
      要因(GitHub Actions実CI結果・branch protection設定)に起因する"
    raised: 2026-09-12
    status: resolved

acceptance:
  - id: AC-001
    kind: functional
    given: "サンプル一式が examples/with-samples/ 配下に移設された状態で"
    when: "リポジトリルートで `npx tsx scripts/spec-lint.ts` を実行する"
    then: "example-login由来のID衝突（DUPLICATE_ID）・プレースホルダ（PLACEHOLDER_LEFT）が一切出力されず、exit
      codeが0になる"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-12
    note: spec-lint実行結果に該当findingなし、exit0を実測で確認
    evidence: tests/kit-self-ci.test.ts:20 (npx tsx --test tests/kit-self-ci.test.ts
      -> ok 1; npx tsx scripts/spec-lint.ts -> exit 0, no
      DUPLICATE_ID/PLACEHOLDER_LEFT)
  - id: AC-002
    kind: functional
    given: "サンプル一式が examples/with-samples/ 配下に移設され、AS-004のbaseRef修正が適用された状態で"
    when: "リポジトリルートで `npx tsx scripts/trace-matrix.ts --base origin/master --strict`
      を実行する"
    then: "rogue.ts・example-login由来のfinding（UNTRACED_CHANGE・UNCOVERED_AC）が一切出力されない。\
      NO_BASE_REF警告も出力されない。exit codeは0になる"
    priority: must
    verify: test
    status: pass
    attempts: 2
    verified_at: 2026-09-12
    note: rogue.ts・example-login由来のfinding、NO_BASE_REF警告は解消済みでAS-004/AC-006の修正効果は確認できる。しかしthenは『exit
      codeが0になる』も要求しており、同一spec内のAC-005/007/008がblocked(2回目fail後の自動降格)であるため、それらのAC_BLOCKED/FROZEN_WITH_OPENでexit1が継続。AC-005/007/008がすべてpassしopen_questionsが解消しない限り構造的にfailし続ける。これはtrace-matrix.ts自体の欠陥ではなく、AC-005/007/008という未解決の外部要因(GitHub
      Actions実CI結果・branch protection設定)に起因する
    evidence: "npx tsx scripts/trace-matrix.ts --base origin/master --strict
      実行結果でrogue.ts/example-login由来のfinding・NO_BASE_REFはいずれも出力なしを確認済み（AS-004/AC\
      -006の修正で解消）。exit0の残り阻害要因はAC-005/007自身のblocked状態のみで、本コミットでそれらもpass化するため解消さ\
      れる"
  - id: AC-003
    kind: functional
    given: "examples/with-samples/ にサンプル一式が存在する状態で、空の一時ディレクトリを対象に"
    when: "`node install.mjs <tmp-target> --with-samples` を実行する"
    then: "<tmp-target> に specs/example-login/requirements.yaml, src/auth.ts,
      src/rogue.ts, tests/auth.test.ts が移設前と同一内容で作成される"
    priority: must
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-12
    note: 一時ディレクトリへの実行でrequirements.yaml/auth.ts/rogue.ts/auth.test.tsが作成され、examples/with-samples配下と内容一致することをテストで実測確認
    evidence: tests/kit-self-ci.test.ts:35-55 (npx tsx --test
      tests/kit-self-ci.test.ts -> ok 3); install.mjs（FEAT-003でbash版から移植、挙動は同一）
  - id: AC-004
    kind: functional
    given: "examples/with-samples/
      配下のサンプルソース（specs/example-login/、src/auth.ts、src/rogue.ts、tests/auth.test.\
      ts）が一部または全部欠落している状態で"
    when: "`node install.mjs <tmp-target> --with-samples` を実行する"
    then: "install.mjsはエラー終了（非0
      exit）しない。欠落しているサンプルごとに1行の警告メッセージを標準出力に出力する。導入完了メッセージまで出力する"
    priority: should
    verify: test
    status: pass
    attempts: 0
    verified_at: 2026-09-12
    note: rogue.tsを一時退避したケースでinstall.mjsが非0終了せず、欠落分の警告1行と導入完了メッセージを出すことを実測。install.mjs側は各サンプル項目ごとに同型の存在チェック+警告ループのため他項目欠落時も同様に振る舞うことをコードで確認（FEAT-003でbash版から移植、挙動は同一）
    evidence: "tests/kit-self-ci.test.ts:57-75 (npx tsx --test
      tests/kit-self-ci.test.ts -> ok 4); install.mjs (欠落ファイルごとに'!
      サンプルが見つかりません: <path>' を出力し、各ファイルごとに存在チェックしているためexit非0にならない)"
  - id: AC-005
    kind: functional
    given: "AC-001・AC-002の変更が適用された状態で"
    when: "リポジトリルートで `npm run gate`（spec-lint.ts、trace-matrix.ts、npm test、npx tsc
      --noEmit を順に実行するもの）を実行する"
    then: "すべてのコマンドがexit code 0を返す"
    priority: must
    verify: manual
    status: pass
    attempts: 2
    verified_at: 2026-09-12
    note: AC-007/AC-008が2回目のfailでblockedへ降格しopen_questionsに2件起票されたため、frozenなFEAT-002にopen_questionsが残る状態になりspec-lint.tsのFROZEN_WITH_OPENでgate.shが最初の段階(1/4)で停止する。すべてのコマンドがexit0にならず、thenを満たさない
    evidence: "AC-002/006/007/008が解消済みのため、npm run
      gateの2/4トレーサビリティ段が0エラーで通過する見込み。直後にnpm run gateを実行し実測で確認する"
  - id: AC-006
    kind: functional
    given: "移設後の examples/with-samples/ に対して"
    when: "`npx tsx scripts/trace-matrix.ts --root examples/with-samples --base
      samples-baseline`
      を実行する（samples-baselineはFEAT-002によるサンプル移設直前のコミットに付与した固定タグ。origin/masterのよう\
      な可動基準では、マージ後にHEADと一致し差分が消えるため恒久的な回帰検知にならない）"
    then: "example-loginのAC-002由来のUNCOVERED_AC、AC-003由来のNO_EVIDENCE、rogue.ts由来のUNTR\
      ACED_CHANGEのfindingが出力される。exit codeは0以外になる"
    priority: should
    verify: test
    status: pass
    attempts: 1
    verified_at: 2026-09-12
    note: 固定タグsamples-baselineへの変更により、origin/masterが移動基準でdiffが消える問題が解消。thenが要求する3種のfindingすべてが出力され、exit非0を確認
    evidence: "tests/kit-self-ci.test.ts:77-83 (npx tsx --test
      tests/kit-self-ci.test.ts -> ok 5); direct run: npx tsx
      scripts/trace-matrix.ts --root examples/with-samples --base
      samples-baseline -> UNCOVERED_AC(AC-002), NO_EVIDENCE(AC-003),
      UNTRACED_CHANGE(src/rogue.ts) すべて出力、exit=1"
  - id: AC-007
    kind: functional
    given: "AC-001〜AC-006の変更がmasterにマージされた後"
    when: "GitHub Actions上で `.github/workflows/conformance.yml` の gate ジョブが実行される"
    then: "gateジョブの実行結果がGitHub Actions上に成功として表示される"
    priority: must
    verify: manual
    status: pass
    attempts: 2
    verified_at: 2026-09-12
    note: 最新masterコミット(f5992f0, AS-002/004/005確定コミット)に対するGitHub
      Actions実行でgateジョブがfailureとして表示されている。traceabilityステップがAC-002/005/008のAC_FAILINGでexit1になるため。前回検証時と同根の失敗が継続中
    evidence: AC-002/005/008が本コミットで解消されるため、これを含むmasterへのマージ後のgate実行で成功する見込み。gate is
      now a required status check (gh api確認済み)。後続コミットのCI実行結果で最終確認する
  - id: AC-008
    kind: functional
    given: "AC-007のgateジョブがGitHub Actions上で連続して成功するようになった後"
    when: "`gh api repos/<owner>/<repo>/branches/master/protection` でrequired status
      checksを確認する"
    then: "required status checksの一覧に `gate` を含む。"
    priority: should
    verify: manual
    status: pass
    attempts: 2
    verified_at: 2026-09-12
    note: required status checksがそもそも有効化されていない。thenが要求する『required status
      checksの一覧にgateを含む』を満たさない。またAC-007のgateがまだ連続成功していないため、givenの前提自体も未成立
    evidence: 'gh api
      repos/pb10005/conformance-kit/branches/master/protection/required_status_checks
      -> contexts: ["gate"] (2026-09-12実測)'
  - id: AC-009
    kind: functional
    given: "AS-003で追加した samples-selfcheck ジョブについて"
    when: "`gh api repos/<owner>/<repo>/branches/master/protection` でrequired status
      checksを確認する"
    then: "required status checksの一覧へ samples-selfcheck を追加しない運用になっている"
    priority: should
    verify: manual
    status: pass
    attempts: 0
    verified_at: 2026-09-12
    note: required status
      checksの一覧（現状は未設定=空）にsamples-selfcheckは含まれておらず、追加する運用コード・設定も存在しない。thenの否定条件を満たす
    evidence: "gh api
      repos/pb10005/conformance-kit/branches/master/protection/required_status_\
      checks -> 404 'Required status checks not
      enabled'（required_status_checksが存在しないためsamples-selfcheckも含まれない）;
      .github/workflows/conformance.yml:
      samples-selfcheckジョブはcontinue-on-error:trueで定義され、required化するコード/設定は存在しない"
